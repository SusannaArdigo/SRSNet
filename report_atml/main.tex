\documentclass[10pt]{article}

\usepackage{atml}

\input{math_commands.tex}

\usepackage{hyperref}
\usepackage{url}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{xcolor}

\graphicspath{{figures/}}

\title{Reproducing SRSNet: Random Controls and a Factorial Audit of
Selective Patching and Adaptive Fusion}

\author{%
  \name Dominik Panzarella \email panzad@usi.ch
  \AND
  \name Susanna Ardig\`o \email ardigs@usi.ch
  \AND
  \name Andre Perugini \email peruga@usi.ch
}

\def\groupid{---}
\def\projectid{SRSNet-Repro}


\begin{document}

\maketitle

\begin{abstract}
We reproduce \emph{Enhancing Time Series Forecasting through Selective
Representation Spaces} (SRSNet, Wu et al., NeurIPS 2025) and audit its
two central design choices --- \emph{Selective Patching} and
\emph{Adaptive Fusion} --- with a paper-faithful pipeline (TFB,
\texttt{batch\_size=64}, \texttt{train\_drop\_last=false}) on the four
ETT datasets and four forecasting horizons. With $176$ valid runs we
confirm the \emph{Adaptive Fusion} ablation of the paper but not the
``state-of-the-art'' claim: DLinear is the strongest baseline on our
hardware (\textsc{Avg MSE} $0.309$ vs SRSNet $0.362$). The plug-and-play
SRS improvement on MLP, extended to the four ETT datasets, beats the
base MLP on $7$ of $16$ cells, with the benefit concentrated on ETTm1
where SRS wins on $4/4$ cells with up to $-4\%$ MSE. We then add a
small-grid \emph{selectivity-controls} study ($2$ datasets, $2$
horizons, $5$ seeds, $9$ variants, $180$ runs, zero failures) that
replaces the learned patch scorer with random selection, with
engineered-feature selection (TASP), with a context-dependent hypernet
fusion (Hypernet-AF), and with an auxiliary pattern-supervision head
(PS-SRS), as well as the three corresponding factorial combinations.
A paired $t$-test over $20$ seed cells per variant yields $p$-values
in $[0.43, 0.95]$ with $95\%$ CIs that all include zero and Cohen's $d
< 0.2$ throughout, so \textbf{at our sample size the experiments are
not powered to demonstrate any variant is superior or inferior to the
baseline}. The right reading is descriptive, not inferential: within
the tested grid, none of the controls or constructive extensions
produces an effect distinguishable from seed-level noise, which is at
best indicative that the learned selector is not clearly necessary in
this regime. The full code and tables are released at
\url{https://github.com/SusannaArdigo/SRSNet}.
\end{abstract}

\section{Introduction}

Patch-based architectures have become the de-facto template for
long-term time series forecasting~\citep{nie2023patchtst,zhang2022crossformer}.
\citet{wu2025srsnet} extend the patching idea with a \emph{Selective
Representation Space} (SRS) that adaptively scores and reassembles
sub-sequences of the input, and combine it with a single-layer MLP
head to produce SRSNet. The paper reports state-of-the-art mean
squared error (MSE) on eight standard forecasting benchmarks and
claims that SRS is a generic plug-and-play module that improves any
patch-based backbone.

This report is a reproduction and a critical audit of those claims.
We replay the official TFB pipeline~\citep{qiu2024tfb} with the
paper's hyper-parameters and report what changes when we swap a
single component at a time --- the learned selective patching, the
free-parameter adaptive fusion, the descriptor head --- including
\emph{random} controls that the paper does not run. The goal is not
to invalidate the architecture but to understand which of its design
choices actually matter on the tested grid.

\section{Scope of reproducibility}
\label{sec:claims}

The paper makes three claims that we can support or reject with
re-runs:

\begin{itemize}
  \item \textbf{C1 -- Headline performance}: SRSNet achieves
    state-of-the-art MSE on the four ETT datasets at horizons
    $\{96, 192, 336, 720\}$
    (\citealt[Table 2]{wu2025srsnet}).
  \item \textbf{C2 -- Ablation hierarchy}: in Table~4 of
    \citet{wu2025srsnet}, removing \emph{Adaptive Fusion} (NoAF) is
    the most damaging ablation, while removing Selective Patching,
    Dynamic Reassembly, or the whole SRS is comparatively milder.
  \item \textbf{C3 -- Plug-and-play SRS}: in Table~3 of
    \citet{wu2025srsnet}, attaching SRS to a vanilla MLP improves
    MSE by approximately $5$--$9\%$.
\end{itemize}

We additionally ask a \textbf{methodological question} the paper does
not address:
\begin{itemize}
  \item \textbf{Q -- Does selectivity matter against a random
    baseline?} The paper's NoSP ablation disables the
    \emph{architecture} of selective patching but keeps the
    bookkeeping. We compare the learned scorer against a strictly
    \emph{random} patch sampler that keeps the rest of the pipeline
    unchanged.
\end{itemize}

\section{Methodology}

\subsection{Model descriptions}
\label{sec:model}

The SRS module is a plug-in that sits between the per-channel
patching step and the patch embedding. Figure~\ref{fig:srs-pipeline}
reproduces the high-level pipeline from
\citet[Fig.~2]{wu2025srsnet}: per-channel Adjacent Patching is paired
with a Selective Patching branch, the two streams are linearly
embedded, fused, and combined with positional embeddings before the
patch-based backbone. Figure~\ref{fig:srs-architecture} reproduces
the per-channel detail from \citet[Fig.~3]{wu2025srsnet}: a
learned \texttt{Scorer$^s$} produces $n$ scores for every candidate
of stride-$1$ patches and selects the $n$ winners via
\texttt{argmax}; a second \texttt{Scorer$^r$} reorders them via
\texttt{argsort}; and an adaptive scalar $\alpha$ controls how much
of the result is mixed into the original patch embedding.

\begin{figure}[t]
  \centering
  \includegraphics[width=0.6\linewidth]{fig1_motivation}
  \caption{Adjacent vs Selective patching (\citealt[Fig.~1]{wu2025srsnet}).
    Adjacent patching uses fixed stride and may straddle
    distribution-shift boundaries; Selective Patching adaptively
    picks sub-sequences from the input.}
  \label{fig:srs-motivation}
\end{figure}

\begin{figure}[t]
  \centering
  \includegraphics[width=0.95\linewidth]{fig2_pipeline}
  \caption{Overall pipeline of the SRS module
    (\citealt[Fig.~2]{wu2025srsnet}). Each channel produces an
    \emph{Adjacent Patching} stream and a \emph{Selective Patching +
    Dynamic Reassembly} stream; the two are linearly embedded and
    combined by an Adaptive Fusion with positional embeddings before
    a patch-based backbone.}
  \label{fig:srs-pipeline}
\end{figure}

\begin{figure}[t]
  \centering
  \includegraphics[width=0.95\linewidth]{fig3_architecture}
  \caption{Detail of the SRS module
    (\citealt[Fig.~3]{wu2025srsnet}). The Selective Patching scans
    candidates with stride $1$, the Scorer$^s$ produces $n$ scores
    per sample, the top-$n$ patches are selected and routed to the
    Dynamic Reassembly. A second scorer produces the reassembly
    order, and the result is fused with the Adjacent Patching
    embedding.}
  \label{fig:srs-architecture}
\end{figure}

The SRSNet model is the SRS module followed by a single-layer linear
forecasting head over the flattened patch tokens; it is the
``Patch-based model'' box on the right of Figure~\ref{fig:srs-pipeline}.
The total parameter count of our baseline configuration with
\texttt{d\_model=256}, \texttt{patch\_len=24}, \texttt{seq\_len=512}
is approximately $31.5$k.

We re-use the authors' official PyTorch implementation released under
the TFB benchmark~\citep{qiu2024tfb}. We did not re-implement the
model: all extensions in Section~\ref{sec:results-ext} are
\emph{single-component} replacements added in
\verb|ts_benchmark/baselines/srs_paper/extensions.py| in our fork.

\subsection{Datasets}

We use the four ETT datasets from
\citet{zhou2021informer}: ETTh1, ETTh2 (1-hour sampling, $17$k rows,
$7$ channels) and ETTm1, ETTm2 (15-min sampling, $69$k rows, $7$
channels). They are obtained from the official TFB release. TFB uses
a $60/20/20$ chronological split on ETT; we keep that split
unchanged. No pre-processing beyond instance normalisation
(\texttt{RevIN}~\citealt{kim2022revin}, already in the SRSNet code)
is applied.

\subsection{Hyperparameters}

We do \emph{not} re-tune hyper-parameters. The SRSNet hyper-parameters
are read from the official shell scripts in
\verb|scripts/multivariate_forecast/<dataset>_script/SRSNet.sh|
released by the authors, with two paper-faithful overrides identified
during the audit phase: \texttt{batch\_size=64} and
\texttt{train\_drop\_last=false}. These are the same overrides
applied to all baselines in our \texttt{lite-paper} and
\texttt{main-compat} scopes. Lookback is the per-dataset value picked
by the vendor shell scripts (\texttt{seq\_len=512} for ETT).

\subsection{Experimental setup and code}

All experiments run through the modified TFB pipeline. The
orchestration is the \verb|scripts/repro/paper_repro.py| runner. It
exposes four scopes:
\begin{itemize}
  \item \texttt{lite-paper}: SRSNet baseline ($16$ cells) plus
    Table~3 plug-in ($40$ cells) plus Table~4 ablation ($40$
    cells) on ETT.
  \item \texttt{main-compat}: seven baseline models
    (PatchTST, DLinear, iTransformer, TimesNet, TimeMixer, PatchMLP,
    xPatch) on ETT, four horizons each.
  \item \texttt{selectivity-controls}: nine variants on a small grid
    of $2$ datasets $\times 2$ horizons $\times 5$ seeds ($180$ runs).
  \item \texttt{selectivity-controls-full}: same nine variants but on
    $4$ datasets $\times 4$ horizons $\times 5$ seeds ($720$ runs,
    not run in this report due to compute budget).
\end{itemize}
The code is released at
\url{https://github.com/SusannaArdigo/SRSNet} on the
\verb|paper-faithful-repro-ett-extensions| branch.

\subsection{Computational requirements}

All experiments ran on a single NVIDIA RTX 4090 (24~GB) instance
provided by Hivenet. The container had a small \texttt{/dev/shm}
(64~MB), which we worked around by forcing
\texttt{num\_workers=0} in the runner. The full set of $176$
reproduction runs plus the $180$ selectivity-control runs took
approximately $14$ wall-clock GPU-hours.

\section{Results}
\label{sec:results}

We separate the reproduction itself (Section~\ref{sec:results-repro})
from the small-grid audit
(Section~\ref{sec:results-controls}--\ref{sec:results-ext}).

\subsection{Results reproducing the original paper}
\label{sec:results-repro}

\paragraph{Claim C1 -- Headline performance.}
On our hardware the average MSE on the $4$ ETT datasets $\times 4$
horizons is summarised in Table~\ref{tab:tab2}. SRSNet achieves
$0.362$, slightly worse than the paper's reported $0.335$, and is
ranked $5/7$ among the baselines we re-ran. DLinear is the strongest
baseline at $0.309$, followed by TimeMixer and iTransformer. The
gap on individual cells averages $|\Delta\text{MSE}| = 18\%$
relative to the paper's Table~8 numbers, with a symmetric pattern:
ETTh1 / ETTm1 are systematically worse than the paper
($+22\%$ / $+14\%$) while ETTh2 / ETTm2 are systematically better
($-18\%$ / $-15\%$). This is consistent with hardware-induced
non-determinism on the cuDNN \texttt{efficient} mode mandated by the
paper's \verb|rolling_forecast_config.json|.

\begin{table}[t]
  \caption{Average MSE across the $4$ ETT datasets and $4$ horizons.
    SRSNet is no longer the best model on our hardware; DLinear is.
    xPatch could not be evaluated under the container's
    \texttt{/dev/shm=64MB} constraint.}
  \label{tab:tab2}
  \begin{center}
  \begin{tabular}{lccr}
    \toprule
    Model & Our AVG MSE & Paper AVG MSE & Cells \\
    \midrule
    \textbf{DLinear} (best on our hw.) & \textbf{0.3091} & 0.348 & 12 \\
    TimeMixer        & 0.3133 & 0.336 & 12 \\
    iTransformer     & 0.3209 & 0.339 & 12 \\
    PatchTST         & 0.3580 & 0.351 & 16 \\
    SRSNet           & 0.3618 & 0.335 & 16 \\
    TimesNet         & 0.3687 & 0.408 & 12 \\
    PatchMLP         & 0.3852 & 0.385 & 8  \\
    \bottomrule
  \end{tabular}
  \end{center}
\end{table}

\paragraph{Claim C2 -- Ablation hierarchy.}
Table~\ref{tab:tab4} reports the SRSNet ablation extended to all four
ETT datasets (the original paper uses only ETTh1 + ETTm2). Averaging
across 16 cells per variant (4 datasets $\times$ 4 horizons), NoAF
remains the most damaging ablation ($+3.4$\% MSE), while NoSRS, NoSP
and NoDR are essentially indistinguishable from the full SRSNet
($\Delta < 0.1$\% MSE on average). The extended grid strengthens
support for C2: even with twice the per-cell coverage, NoAF stays
isolated as the only critical component.

\begin{table}[t]
  \caption{Reproduction of Table~4 of \citet{wu2025srsnet}, extended
    to the four ETT datasets (16 cells per variant). NoAF is the
    only ablation with a clearly visible MSE drop; the other three
    ablations differ from the full SRSNet by less than $0.1\%$ on
    average and are within the seed-level noise band (no paired
    significance test was run on this table).}
  \label{tab:tab4}
  \begin{center}
  \begin{tabular}{lcr}
    \toprule
    Variant          & Avg MSE & $\Delta$ vs Full \\
    \midrule
    SRSNet (Full)    & 0.3616 & $0$\% \\
    SRSNet\_NoSRS    & 0.3616 & $+0.0$\% \\
    SRSNet\_NoSP     & 0.3614 & $-0.1$\% \\
    SRSNet\_NoDR     & 0.3617 & $+0.0$\% \\
    \textbf{SRSNet\_NoAF}     & \textbf{0.3740} & $\mathbf{+3.4}$\% \\
    \bottomrule
  \end{tabular}
  \end{center}
\end{table}

\paragraph{Claim C3 -- Plug-and-play SRS.}
The paper reports an average $5$--$9\%$ MSE improvement when
attaching SRS to a vanilla MLP head (Table~3 of
\citealt{wu2025srsnet}). On our setup, extended to all four ETT
datasets, the SRS plug-in improves the base MLP on $7$ of $16$
cells; the average effect is close to zero (see
Table~\ref{tab:tab3}). The benefit is concentrated on ETTm1, where
the plug-in beats the base MLP on $4/4$ cells with up to $-4$\% MSE;
on ETTh1 / ETTh2 / ETTm2 the effect is mixed and at most marginal.
C3 is partially supported, but only on ETTm1.

\begin{table}[t]
  \caption{Reproduction of Table~3 of \citet{wu2025srsnet}, extended
    to the four ETT datasets. SRS as a plug-in on top of a vanilla
    MLP improves the base on 7 of 16 cells. The effect is
    concentrated on ETTm1.}
  \label{tab:tab3}
  \begin{center}
  \small
  \begin{tabular}{lcccc}
    \toprule
    Dataset / H & Base MSE & +SRS MSE & $\Delta$\% & Improves? \\
    \midrule
    ETTh1 / 96   & 0.4360 & 0.4362 & $+0.03$ & no \\
    ETTh1 / 192  & 0.4815 & 0.4869 & $+1.13$ & no \\
    ETTh1 / 336  & 0.5396 & 0.5289 & $-1.99$ & \textbf{yes} \\
    ETTh1 / 720  & 0.6535 & 0.6580 & $+0.69$ & no \\
    ETTh2 / 96   & 0.2171 & 0.2268 & $+4.46$ & no \\
    ETTh2 / 192  & 0.2755 & 0.2736 & $-0.68$ & \textbf{yes} \\
    ETTh2 / 336  & 0.3126 & 0.3231 & $+3.36$ & no \\
    ETTh2 / 720  & 0.4191 & 0.4231 & $+0.95$ & no \\
    \textbf{ETTm1 / 96}  & 0.3220 & 0.3185 & $\mathbf{-1.09}$ & \textbf{yes} \\
    \textbf{ETTm1 / 192} & 0.3782 & 0.3680 & $\mathbf{-2.68}$ & \textbf{yes} \\
    \textbf{ETTm1 / 336} & 0.4313 & 0.4139 & $\mathbf{-4.04}$ & \textbf{yes} \\
    \textbf{ETTm1 / 720} & 0.5058 & 0.5057 & $-0.03$ & \textbf{yes} \\
    ETTm2 / 96   & 0.1492 & 0.1478 & $-0.88$ & \textbf{yes} \\
    ETTm2 / 192  & 0.1809 & 0.1830 & $+1.16$ & no \\
    ETTm2 / 336  & 0.2144 & 0.2193 & $+2.26$ & no \\
    ETTm2 / 720  & 0.2683 & 0.2775 & $+3.42$ & no \\
    \bottomrule
  \end{tabular}
  \end{center}
\end{table}

\subsection{Selectivity-controls study}
\label{sec:results-controls}

The paper's NoSP ablation removes the architecture of selective
patching but keeps the rest of the SRS pipeline intact. A stricter
control is to replace the learned scorer with random patch
sampling: the rest of the module (Dynamic Reassembly, Adaptive
Fusion, embedding) is untouched.

We implement two variants in
\verb|extensions.py|: \texttt{SRSNet\_RandomSP} (random select +
learned shuffle) and \texttt{SRSNet\_RandomSPNoShuffle} (random
select + identity shuffle). We retrain end-to-end on
ETTh1+ETTm2 $\times$ H$\in\{96, 720\}$ $\times$ $5$ seeds, so the
shape of the network is unchanged but the selection function is
randomised.

Across the $20$ resulting cells per variant the mean $\Delta$MSE is
$-0.22\%$ (RandomSP) and $-0.12\%$ (RandomSPNoShuffle); the
seed-level standard deviation is $1.23\%$ and $1.46\%$ (paired
$t$-test against the null $\Delta = 0$: $p = 0.43$ and $0.72$;
$95\%$ CIs are $[-0.79, +0.35]$ and $[-0.80, +0.57]$ respectively;
Cohen's $d$ is $0.18$ and $0.08$). \textbf{Neither effect is
statistically significant at our sample size}. The right reading is
descriptive: random patch selection is empirically indistinguishable
from the learned scorer on the tested grid and beats it on
$12/20$ seeds, but the test cannot reject the null at $\alpha=0.05$.
The strongest single cell is ETTh1 / H=720, where RandomSPNoShuffle
is better than SRSNet on $5/5$ seeds with mean
$\Delta = -1.07\%$ -- still a per-cell observation rather than a
significance claim.

\subsection{Constructive extensions and factorial decomposition}
\label{sec:results-ext}

To complement the negative-control result with a constructive
proposal, we add three extensions mapped to Future Work entries of
\citet[Sec.~6]{wu2025srsnet}, and the corresponding factorial
combinations with the Hypernet-AF fusion. The full list is in
Table~\ref{tab:variants}.

\begin{table}[t]
  \caption{The nine selectivity-controls variants. ``Free $\alpha$''
    is the paper's adaptive-fusion parameter; ``Hypernet $\alpha$''
    is our zero-initialised hypernet over a batch context.}
  \label{tab:variants}
  \begin{center}
  \begin{tabular}{lcccc}
    \toprule
    Variant & \texttt{\_select} & \texttt{\_shuffle} & $\alpha$ fusion & aux loss \\
    \midrule
    SRSNet (baseline)               & learned   & learned  & free     & --   \\
    SRSNet\_RandomSP                & random    & learned  & free     & --   \\
    SRSNet\_RandomSPNoShuffle       & random    & identity & free     & --   \\
    SRSNet\_TASP                    & engineered & learned & free     & --   \\
    SRSNet\_HypernetAF              & learned   & learned  & hypernet & --   \\
    SRSNet\_PSRS                    & learned   & learned  & free     & yes  \\
    SRSNet\_RandomSP\_HypernetAF    & random    & learned  & hypernet & --   \\
    SRSNet\_TASP\_HypernetAF        & engineered & learned & hypernet & --   \\
    SRSNet\_PSRS\_HypernetAF        & learned   & learned  & hypernet & yes  \\
    \bottomrule
  \end{tabular}
  \end{center}
\end{table}

\paragraph{TASP -- Time-Aware Selective Patching.}
Replaces the \texttt{Scorer$^s$} MLP over raw patch values with a
$4{\to}16{\to}\text{patch\_num}$ MLP over engineered per-window
statistics (dominant non-DC FFT magnitude, lag-1 autocorrelation,
within-window variance, linear trend slope). The scorer parameter
count drops from $6{,}038$ (vanilla) to $374$; the total module
parameter count drops from $27{,}287$ to $21{,}703$ ($-20\%$),
so any improvement cannot be attributed to extra capacity. Targets
FW\#1 (environment-aware mechanism) and partially L3
(interpretability) -- the scorer's \emph{inputs} are named, but the
$4{\to}16{\to}\text{patch\_num}$ MLP body that maps them to a score
remains opaque.

\paragraph{Hypernet-AF.}
Replaces the free $\alpha$ tensor of shape \texttt{[patch\_num,
d\_model]} with a tiny hypernet (\texttt{d\_model$\to$8$\to$patch\_num$\cdot$d\_model})
that consumes a batch-level context vector. Hypernet weights are
zero-initialised and the output bias is set to $3.5$, so step~0
behaves exactly as vanilla SRSNet ($\sigma(3.5) \approx 0.97$).
Any deviation is therefore attributable to learned context
dependence, not to extra capacity at initialisation.
\textbf{Capacity caveat:} the hypernet has $50{,}688$ parameters
versus $5{,}376$ for the free $\alpha$ tensor it replaces, so the
total module grows from $27{,}287$ to $74{,}399$ parameters
($+173\%$). The "parameter-matched" framing earlier in this work
referred to the discarded GAF baseline, \emph{not} to the vanilla
SRS; versus vanilla, Hypernet-AF is capacity-inflated and its
factorial combinations inherit the inflation. The context is also
a batch-level mean (\texttt{e\_orig.mean(0).mean(0)}), so the same
$\alpha$ is applied to every sample in a batch; this is a
``batch-aware'' rather than truly ``sample-aware'' fusion. Targets
FW\#3 (more efficient update mechanism for $\alpha$) and L4
(initialisation); we acknowledge that "compute mechanism" is a
looser reading of FW\#3 than "update mechanism".

\paragraph{PS-SRS -- Pattern-Supervised SRS.}
Adds an auxiliary head on top of the scorer's first hidden activation
that predicts three engineered descriptors (FFT max, lag-1
autocorrelation, within-window variance) computed from each
candidate window. The auxiliary loss is scaled by $\lambda=10^{-2}$
and exposed via \verb|out_loss["additional_loss"]|, which is added
to the main prediction loss by the \verb|DeepForecastingModelBase|
training loop natively.
\textbf{Two important caveats.} First, we map this to paper FW\#4
(``supervised module for sample-wise patterns''), but the paper's
exact phrasing is ``constructing a more explicit optimization
objective \emph{between data patterns and $\alpha$}''. Our PS-SRS
supervises the \emph{scorer}, not $\alpha$, so it is a partial
re-interpretation rather than a direct realisation. Second, in our
audit we found that the three descriptors live at very different
scales (FFT max $\approx O(10)$, AC1 in $[-1, 1]$, variance
$\approx O(1)$): the unweighted MSE used by v1 of PS-SRS was
dominated by the FFT term so the auxiliary head effectively
predicted only FFT max. After the audit we z-scored every descriptor
across the batch and made $\lambda$ a hyper-parameter; the lambda
sweep
(Section~\ref{sec:psrs-sweep}) reports the v2 results.

\paragraph{Factorial design.}
The three combinations \texttt{SRSNet\_RandomSP\_HypernetAF},
\texttt{SRSNet\_TASP\_HypernetAF} and \texttt{SRSNet\_PSRS\_HypernetAF}
fill the missing cells of the $4\times 2$ (Select, Fusion) factorial
table:

\medskip

\noindent
\begin{tabular}{l|cc}
  Select \textbackslash\ Fusion & Free $\alpha$ & Hypernet $\alpha$ \\
  \hline
  Learned (vanilla)            & SRSNet & SRSNet\_HypernetAF \\
  Random                       & SRSNet\_RandomSP & SRSNet\_RandomSP\_HypernetAF \\
  TASP (engineered)            & SRSNet\_TASP & SRSNet\_TASP\_HypernetAF \\
  LearnedAux (PSRS)            & SRSNet\_PSRS & SRSNet\_PSRS\_HypernetAF \\
\end{tabular}

\medskip

\paragraph{Results.}
Figure~\ref{fig:variant-ranking} ranks the eight extensions by mean
$\Delta$MSE versus SRSNet, and Table~\ref{tab:variant-stats} reports
the corresponding paired $t$-test statistics for all $11$ variants
(four 1-axis changes including RandomSPNoShuffle, three 2-axis
combos, and three new 3-axis combos that stack identity shuffle on
top of the (Select, Hypernet-AF) combos).  Every variant has $p >
0.29$, all $95\%$ CIs include zero, and the absolute Cohen's $d$
never exceeds $0.24$. \textbf{No variant is statistically
distinguishable from the baseline at our sample size}, and a
required-$n$ calculation indicates we would need $265$ to $650$
seed cells per variant to detect a $0.2\%$ mean $\Delta$MSE with
$80\%$ power at $\alpha = 0.05$ (we have $20$). Any reading beyond
"effects are within the seed-level noise band" would be premature.

The PSRS and PSRS\_HypernetAF rows in
Table~\ref{tab:variant-stats} use the patched (v2) auxiliary loss
with $\lambda = 10^{-2}$ and z-scored targets; the
\verb|selectivity_controls.csv| file on disk has been rebuilt with
those cells so that the published CSV is consistent with the
current code path.  Earlier versions of this work reported the v1
broken values ($+0.22\%$ for PSRS, $+0.12\%$ for PSRS\_HypernetAF);
after the fix the same lambda yields $-0.27\%$ and $+0.16\%$
respectively, which moves PSRS from the worst-mean position to the
best-mean position among 1-axis variants.  This sign flip is
consistent with the audit diagnosis (Section~\ref{sec:psrs-sweep}):
the v1 unweighted MSE was dominated by the FFT term and was
training the head to predict the wrong thing.

\begin{table}[t]
  \caption{Paired $t$-test of each variant against the SRSNet
    baseline across $20$ (dataset, horizon, seed) cells. The 11
    variants cover three 1-axis changes (Random / TASP / HypernetAF /
    PSRS), three 2-axis (Select $\times$ Fusion) combos, three new
    3-axis combos (Select $\times$ Identity-Shuffle $\times$
    Hypernet-AF), and the RandomSPNoShuffle control.  PSRS uses the
    z-scored auxiliary loss with $\lambda = 10^{-2}$ (the patched
    default).  \textbf{None of the variants reaches $p < 0.05$};
    effect sizes are all small in the Cohen's $d$ sense ($|d| < 0.3$).}
  \label{tab:variant-stats}
  \begin{center}
  \small
  \begin{tabular}{lcccc}
    \toprule
    Variant & mean $\Delta$\% (std) & $95\%$ CI & $p$ & Cohen's $d$ \\
    \midrule
    \multicolumn{5}{l}{\emph{1-axis changes}} \\
    SRSNet\_RandomSP                          & $-0.22$ ($1.23$) & $[-0.79, +0.35]$ & $0.43$ & $-0.18$ \\
    SRSNet\_RandomSPNoShuffle                 & $-0.12$ ($1.46$) & $[-0.80, +0.57]$ & $0.72$ & $-0.08$ \\
    SRSNet\_HypernetAF                        & $-0.09$ ($1.44$) & $[-0.76, +0.58]$ & $0.78$ & $-0.06$ \\
    \textbf{SRSNet\_PSRS} (z-scored)          & $\mathbf{-0.27}$ ($1.14$) & $[-0.81, +0.26]$ & $0.30$ & $-0.24$ \\
    SRSNet\_TASP                              & $+0.25$ ($1.82$) & $[-0.60, +1.10]$ & $0.54$ & $+0.14$ \\
    \midrule
    \multicolumn{5}{l}{\emph{2-axis combos (Select $\times$ Fusion)}} \\
    SRSNet\_RandomSP\_HypernetAF              & $-0.20$ ($1.65$) & $[-0.98, +0.57]$ & $0.59$ & $-0.12$ \\
    SRSNet\_TASP\_HypernetAF                  & $-0.02$ ($1.23$) & $[-0.60, +0.56]$ & $0.95$ & $-0.02$ \\
    SRSNet\_PSRS\_HypernetAF (z-scored)       & $+0.16$ ($1.05$) & $[-0.33, +0.65]$ & $0.50$ & $+0.15$ \\
    \midrule
    \multicolumn{5}{l}{\emph{3-axis combos (Select $\times$ Identity-Shuffle $\times$ Hypernet-AF)}} \\
    SRSNet\_RandomSP\_NoShuffle\_HypernetAF   & $-0.24$ ($1.53$) & $[-0.96, +0.47]$ & $0.49$ & $-0.16$ \\
    SRSNet\_PSRS\_NoShuffle\_HypernetAF       & $+0.05$ ($1.22$) & $[-0.52, +0.63]$ & $0.85$ & $+0.04$ \\
    SRSNet\_TASP\_NoShuffle\_HypernetAF       & $+0.07$ ($0.97$) & $[-0.38, +0.53]$ & $0.74$ & $+0.08$ \\
    \bottomrule
  \end{tabular}
  \end{center}
\end{table}

\begin{figure}[t]
  \centering
  \includegraphics[width=0.95\linewidth]{fig5_variant_ranking}
  \caption{Mean $\Delta$MSE versus SRSNet baseline across $20$ cells
    per variant (selectivity-controls study). Error bars are the
    seed-level standard deviation. All means lie inside the noise
    band; no extension is statistically distinguishable from the
    baseline.}
  \label{fig:variant-ranking}
\end{figure}

Figure~\ref{fig:factorial} shows the $(Select, Fusion)$ factorial
table. The best mean MSE in the grid is achieved by
$(Random, \text{Free }\alpha) = 0.3773$, marginally better than the
$(Learned, \text{Free }\alpha) = 0.3788$ baseline. The Fusion main
effect is $-0.10$\% (std $1.28$, $n=80$), the Select main effects
are $-0.17$\% (Learned $\to$ Random), $+0.17$\% (Learned $\to$
TASP), $+0.22$\% (Learned $\to$ LearnedAux). All four effects are
inside the seed-level std band; consistent with the per-variant
$t$-tests in Table~\ref{tab:variant-stats}, none of the factor
contrasts is statistically distinguishable from zero, and the
factorial decomposition is most honestly summarised as
\emph{absence of detectable main effects on either factor at the
tested sample size}.

\begin{figure}[t]
  \centering
  \includegraphics[width=0.7\linewidth]{fig4_factorial}
  \caption{Factorial decomposition over $20$ cells per variant.
    Each cell shows mean MSE and $\Delta$\% relative to the
    $(Learned, \text{Free }\alpha)$ baseline. Colour scale is
    saturated at $\pm 0.5$\%; all cells are within $\pm 0.30$\%
    so the heat map is mostly grey, consistent with null main
    effects on both factors.}
  \label{fig:factorial}
\end{figure}

The most informative cell is ETTh1 / H=720
(Figure~\ref{fig:etth1h720}): two variants beat the SRSNet baseline
on $5/5$ seeds, and both \emph{remove} or \emph{simplify} the
learned scorer (RandomSPNoShuffle, $-1.07$\%; TASP, $-1.00$\%).
None of the variants that \emph{refine} the learned scorer (PSRS,
HypernetAF) improve over the baseline on this cell.

\begin{figure}[t]
  \centering
  \includegraphics[width=0.85\linewidth]{fig6_etth1_h720}
  \caption{ETTh1 H=720 -- mean MSE across $5$ seeds, ranked
    ascending. Two variants beat the baseline on $5/5$ seeds:
    \texttt{RandomSPNoShuffle} ($-1.07$\%) and \texttt{TASP}
    ($-1.00$\%). All variants that refine the learned scorer
    (PSRS, HypernetAF) sit at or above the baseline.}
  \label{fig:etth1h720}
\end{figure}

\section{Discussion}

The reproduction confirms one of the paper's claims (C2: NoAF is
the dominant ablation), partially supports another (C1: SRSNet is
in the top half of baselines but DLinear is strictly better on
ETT), and partially supports a third (C3: the SRS plug-and-play
improvement on MLP is concentrated on ETTm1, where it wins on
$4/4$ cells, while remaining mixed elsewhere).

For the selectivity-controls study and the constructive extensions,
the honest reading is more restrained than ``learned selection is
not necessary''. None of the variants we tested rejects the null
hypothesis of zero mean $\Delta$MSE at the standard $\alpha = 0.05$
level (Table~\ref{tab:variant-stats}). Read \emph{descriptively},
random patch selection matches or beats the learned scorer on
$12/20$ seeds and the factorial decomposition shows no detectable
main effect on either factor; read \emph{inferentially}, our sample
size is too small to support any positive or negative claim about
selectivity, fusion, or pattern supervision in this regime.

\subsection{What our experiments do and do not show}

Three statements we believe are well-supported:
\begin{enumerate}
  \item \textbf{Within the tested grid (4 ETT cells, 5 seeds), the
    learned selector is not \emph{clearly} superior to a random
    selector}. The mean $\Delta$MSE is small in magnitude and lies
    inside the seed-level noise band. We cannot prove that the two
    are equivalent; we can prove that we cannot reject the null at
    our sample size.
  \item \textbf{The Adaptive Fusion is the only Tab.4 ablation
    with a clearly visible MSE drop ($+3.4\%$ on average)}. This is
    a positive finding that survives the extended grid.
  \item \textbf{Hypernet-AF carries $173\%$ more parameters than the
    free $\alpha$ tensor it replaces.} The "parameter-matched"
    framing we used early on referred to the discarded GAF, not to
    vanilla SRS, and we correct it here.
\end{enumerate}

Three statements we explicitly do \emph{not} make:
\begin{enumerate}
  \item Random selection is as good as or better than learned
    selection. Indistinguishability at $n = 20$ is not equivalence.
  \item PS-SRS faithfully implements paper FW\#4. The paper's
    phrasing ties patterns to $\alpha$; we tied them to the
    scorer, which is a related but different mechanism.
  \item The lambda we used for PS-SRS ($10^{-2}$) was optimal.
    Section~\ref{sec:psrs-sweep} reports a small sweep that suggests
    the picked value was not principled.
\end{enumerate}

\subsection{Why PS-SRS likely degrades the forecasting accuracy}
\label{sec:psrs-sweep}

The audit identified two issues with v1 of PS-SRS: (a) the three
descriptor targets (FFT max in $[2.5, 19]$, AC1 in $[-1, 1]$,
variance in $[0.08, 2.25]$) have wildly different magnitudes, so
the unweighted MSE was dominated by the FFT term; (b) the
$\lambda_\text{aux} = 10^{-2}$ default was never swept. To probe
how much these design choices contributed to the negative result,
we ran a 160-task sweep ($\lambda \in \{0,\,10^{-3},\,10^{-2},\,
10^{-1}\}$) with z-scored descriptor targets, for both PS-SRS
standalone and the PSRS\_HypernetAF combo, across the small
selectivity grid (2 datasets x 2 horizons x 5 seeds = 20 cells per
$(\text{variant}, \lambda)$). The intent is descriptive: the goal
is not to find a winning $\lambda$ but to verify whether
\emph{any} choice of $\lambda$ recovers an improvement over
baseline. Results are in Table~\ref{tab:psrs-sweep}.

\begin{table}[ht]
  \caption{PS-SRS $\lambda$ sweep with z-scored auxiliary targets.
    Each row is $n=20$ cells. The mean $\Delta$\% is the paired
    delta vs the SRSNet baseline on the same
    (dataset, horizon, seed) cells. All $p$-values exceed
    $0.05$; the closest to significance is PS-SRS with
    $\lambda = 10^{-1}$ at $p = 0.054$, which is a
    \emph{degradation} ($+0.64\%$) rather than an improvement,
    confirming the main-loss conflict diagnosed in the audit.}
  \label{tab:psrs-sweep}
  \begin{center}
  \small
  \begin{tabular}{llrcccr}
    \toprule
    Variant & $\lambda$ & mean $\Delta$\% & std \% & $95\%$ CI & $p$ & wins/20 \\
    \midrule
    PS-SRS                          & $0$       & $+0.158$ & $1.11$ & $[-0.36, +0.68]$ & $0.534$ & $10$ \\
    PS-SRS                          & $10^{-3}$ & $+0.432$ & $1.24$ & $[-0.15, +1.01]$ & $0.136$ & $9$  \\
    \textbf{PS-SRS}                 & $\mathbf{10^{-2}}$ & $\mathbf{-0.274}$ & $1.14$ & $[-0.81, +0.26]$ & $0.296$ & $\mathbf{13}$ \\
    PS-SRS                          & $10^{-1}$ & $+0.644$ & $1.40$ & $[-0.01, +1.30]$ & $\mathbf{0.054}$ & $8$  \\
    \midrule
    \textbf{PSRS\_HypernetAF}        & $\mathbf{0}$       & $\mathbf{-0.144}$ & $0.94$ & $[-0.59, +0.30]$ & $0.502$ & $\mathbf{12}$ \\
    PSRS\_HypernetAF                 & $10^{-3}$ & $-0.089$ & $0.98$ & $[-0.55, +0.37]$ & $0.688$ & $11$ \\
    PSRS\_HypernetAF                 & $10^{-2}$ & $+0.162$ & $1.05$ & $[-0.33, +0.65]$ & $0.499$ & $12$ \\
    PSRS\_HypernetAF                 & $10^{-1}$ & $-0.089$ & $1.01$ & $[-0.56, +0.38]$ & $0.697$ & $11$ \\
    \bottomrule
  \end{tabular}
  \end{center}
\end{table}

Three observations from the sweep matter.

First, the v1 of PS-SRS that we reported in
Table~\ref{tab:variant-stats} (mean $\Delta = +0.22\%$, $n=20$)
corresponds to the unfixed $\lambda = 10^{-2}$ row with no
z-scoring; with z-scoring the same $\lambda$ moves to
$\Delta = -0.27\%$, the same magnitude with the opposite sign.
This confirms that the v1 negative result was at least partly a
consequence of the FFT-dominated descriptor MSE, not a structural
property of the auxiliary supervision idea.

Second, no $\lambda$ value reaches $p < 0.05$ for either variant.
The closest is $\lambda = 10^{-1}$ for PS-SRS standalone with
$p = 0.054$ -- but this is a \emph{degradation} of $+0.64\%$, not
an improvement. That borderline-significant drop is the
quantitative version of the gradient-conflict diagnosis from the
audit: with $\lambda$ large enough to dominate, the auxiliary
loss pulls the scorer hidden activation away from the direction
useful for forecasting.

Third, the two variants disagree on where the best $\lambda$ lies.
PS-SRS standalone is best at $\lambda = 10^{-2}$
(after the z-scoring fix); PSRS\_HypernetAF is best at
$\lambda = 0$. We attribute the difference to capacity: the
Hypernet-AF combo already brings $2.7\times$ more parameters
than the vanilla baseline, so the additional capacity to
``absorb'' descriptor patterns already exists in the fusion
hypernet and the auxiliary head no longer brings useful
information.

The aux gradient also propagates back into the scorer's hidden
activation, so the optimiser has to split that layer's capacity
between predicting descriptors and producing useful selection
scores. We measured the cosine similarity between the
$\nabla_W \mathrm{aux}$ and $\nabla_W \mathrm{main}$ on the first
scorer layer and obtained $\cos = 0.05$ at step~0 (essentially
orthogonal). Z-scoring the targets reduces the magnitude conflict
but does not align the gradient directions, so an irreducible
fraction of the scorer capacity is still ``stolen'' by the aux head.
This matches the intuition that PS-SRS in its current form is
likely to compete with, rather than complement, the main
forecasting objective.

The aux gradient also propagates back into the scorer's hidden
activation, so the optimiser has to split that layer's capacity
between predicting descriptors and producing useful selection
scores. We measured the cosine similarity between the
$\nabla_W \mathrm{aux}$ and $\nabla_W \mathrm{main}$ on the first
scorer layer and obtained $\cos = 0.05$ at step~0 (essentially
orthogonal). Z-scoring the targets reduces the magnitude conflict
but does not align the gradient directions, so an irreducible
fraction of the scorer capacity is still ``stolen'' by the aux head.
This matches the intuition that PS-SRS in its current form is
likely to compete with, rather than complement, the main
forecasting objective.

\subsection{What was easy}

The TFB harness is well-documented and the authors release usable
shell scripts per dataset. After one round of paper-faithful fixes
(batch size, drop-last flag, num\_workers, the
\texttt{np.Inf}/\texttt{np.inf} renaming and a downgrade of pandas
below 2.2 for the \texttt{'H'}/\texttt{'h'} frequency change) the
SRSNet rows produced numbers within $1$--$3$\% of the paper. The
Table~4 ablation reproduces qualitatively, which makes the
NoAF-is-critical claim defensible without any heroic engineering.

\subsection{What was difficult}

The compute environment was hostile in unexpected ways. The first
RTX 4090 box on Hivenet had \texttt{/dev/shm=64MB}, which prevents
TFB's default DataLoader workers from running; the workaround was
forcing \texttt{num\_workers=0}, which works but is slower. Several
SSH sessions to the bastion dropped mid-batch, so the runner had to
be \texttt{nohup}-detached and the orchestration script had to be
fully resume-aware (idempotent metadata + per-task config hash).
Lightning AI as a backup ran into a similar set of paper cuts
(\texttt{np.Inf}, pandas 3.0, missing \texttt{matplotlib} /
\texttt{dash}). Finally, the cuDNN \texttt{efficient} setting that
the paper's \verb|rolling_forecast_config.json| selects is
non-deterministic, so there is a residual seed-and-hardware noise
floor (about $\pm 1$--$2$\% MSE) below which no extension claim can
be honestly made on this grid.

\subsection{Communication with original authors}

We did not contact the original authors. The code release on
GitHub and the supplementary appendix were sufficient to identify
all relevant hyper-parameters; the residual gap between our
reproduction and the paper numbers is consistent with hardware /
seed effects and does not require author intervention.

\section{Conclusion}

We reproduced SRSNet's main experiments under a paper-faithful
TFB pipeline on the four ETT datasets and four horizons. The
reproduction supports the \emph{Adaptive Fusion} ablation
($+3.4\%$ MSE when AF is removed, averaged across $16$ cells), is
in tension with the headline ``state-of-the-art'' framing (DLinear
beats SRSNet on average MSE), and partially supports the
plug-and-play SRS claim (concentrated on ETTm1, $4/4$ cells; mixed
elsewhere). We then added a small-grid selectivity-controls audit
with nine variants ($180$ runs, zero failures) and a $4\times 2$
factorial design over (Select, Fusion).

\textbf{Read inferentially}, the small-grid audit is inconclusive:
no variant rejects the null at $\alpha = 0.05$, all confidence
intervals include zero, all effect sizes are small.
\textbf{Read descriptively}, the audit is consistent with a
hypothesis worth probing at larger scale --- that on ETT, the
learned selector and the free-parameter fusion are not the
mechanisms responsible for SRSNet's performance --- but it cannot
establish that hypothesis at our sample size. The lambda sweep on
PS-SRS confirms that the negative result for that variant has at
least two technical reasons (unweighted descriptor MSE dominated
by the FFT term; gradient interference on the scorer's hidden
activation) beyond any conceptual flaw of the proposal.

We release the code, the manifests, and the full set of
CSV / Markdown report tables at
\url{https://github.com/SusannaArdigo/SRSNet}.

The natural follow-up is to extend the selectivity-controls study
to the full $4$-dataset $\times 4$-horizon grid and to the
remaining benchmarks of the paper (Weather, Electricity, Solar,
Traffic), and to add a statistical-significance framework
(paired Wilcoxon + bootstrap CI + effect size) so each claim of
the form ``X improves Y by Z\%'' is accompanied by a $p$-value and
a confidence interval. A required-$n$ calculation under the
observed seed noise of $\sigma \approx 1.4\%$ suggests $n \in
[270, 550]$ cells per variant to detect a $0.2\%$ effect at
$80\%$ power, against the $20$ cells per variant we ran.

\section*{Member contributions}

Both authors contributed to the design of the experiments, the
selection of the selectivity-controls variants, and the writing of
the report. The code, the runner infrastructure, and the
generation of the CSV/Markdown tables were a shared effort.

\bibliography{main}
\bibliographystyle{tmlr}

\include{appendix}

\end{document}
